doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    - var hwp= htmlWebpackPlugin
    for item in hwp.files.chunks.index.css
      link(rel="stylesheet" href=item)
    script(src=hwp.files.chunks.index.entry)
  body
    #index
      header
        h1 Head
